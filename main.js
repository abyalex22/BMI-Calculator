function calculate(){
    // store user height
    const height = (userHeight.value*0.01)
    console.log(height);
    
    const weight = userWeight.value     
        const bmi = (weight/(height)**2);
        userBMI.value =`${bmi}kg/m^2 ` 
}